apiVersion: apps/v1
kind: Deployment
metadata:
  name: management-postgresdb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: management-postgresdb
  template:
    metadata:
      labels:
        app: management-postgresdb
    spec:
      containers:
      - name: management-postgresdb
        image: postgres
        resources:
          limits:
            cpu: "0.1"
            memory: "512Mi"
          requests:
            cpu: "0.1"
            memory: "128Mi"
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_PASSWORD 
          value: "postgres*8"